(function(){const notification=document.createElement("div");notification.id="network-status-notification";notification.innerHTML=`\n        <div class="icon"><i class="fas fa-wifi"></i></div>\n        <div class="message">Your internet connection seems slow.</div>\n        <button class="close-btn" aria-label="Close notification">&times;</button>\n    `;document.body.appendChild(notification);const closeBtn=notification.querySelector(".close-btn");const messageNode=notification.querySelector(".message");const iconNode=notification.querySelector(".icon i");closeBtn.addEventListener("click",()=>{notification.classList.remove("show")});function updateStatus(){const isOnline=navigator.onLine;const connection=navigator.connection||navigator.mozConnection||navigator.webkitConnection;let showMessage=false;let messageText="";let isOffline=false;if(!isOnline){showMessage=true;messageText="You are currently offline. Check your connection.";isOffline=true}else if(connection){if(["slow-2g","2g","3g"].includes(connection.effectiveType)){showMessage=true;messageText="Weak internet connection detected. Media might load slowly."}}if(showMessage){messageNode.textContent=messageText;notification.classList.toggle("offline",isOffline);iconNode.className=isOffline?"fas fa-plane":"fas fa-wifi";notification.classList.add("show")}else{notification.classList.remove("show")}}updateStatus();window.addEventListener("online",updateStatus);window.addEventListener("offline",updateStatus);const connection=navigator.connection||navigator.mozConnection||navigator.webkitConnection;if(connection){connection.addEventListener("change",updateStatus)}setInterval(updateStatus,1e4)})();